# Web-Crawler
Async Web Crawler with Concurrency Control  A Node.js crawler that fetches multiple URLs in parallel, respects a concurrency limit (semaphore pattern), and aggregates results in real time.
